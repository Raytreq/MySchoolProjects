#include <iostream>
#include <time.h> 
#include <cstdlib>
using namespace std;


int main() {
    
    int n;
    int parz = 0; 
    int nieparz  = 0;
    
    do{
        cout<<"Podaj do jakiej liczby mam losować <100;200>, która jest podzielna przez 10: ";
        cin>>n;
    }while(!(n>=100 && n<=200 && n%10==0));
    
    while(n<100 || n>200 || n%10!=0)
        srand(time(NULL));
        
    int ilosc[n+1];
    
    for(int i=0;i<=n;i++)
        ilosc[i]=0;
        
    int liczby[100];
    
    for(int i=0;i<=100;i++){
        liczby[i]=rand()%(n+1);
        ilosc[liczby[i]]++;   
    }
    
    for(int i=0;i<=100;i++) 
        cout<<liczby[i]<<" ";
        
    for(int i=0;i<=n;i++)
        cout<<"Liczba "<<i<<": "<<ilosc[i]<<endl;
        
    for(int i=0;i<=n;i++)
    
    for (i=0; i<=n; i++) {
        if (i%2 == 0){
            parz++;
        }
        else {
            nieparz++;
        }
    }
    
    cout << "Parzyste liczby: " << parz << endl;
    cout << "Nieparzyste liczby: " << nieparz << endl;
        
    return 0;
    
}