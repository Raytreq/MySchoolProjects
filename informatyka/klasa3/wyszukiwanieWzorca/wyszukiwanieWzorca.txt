#include <iostream>
using namespace std;

// int Znajdz(string w, string t) {
//     int dw=w.size();
//     int dt=t.size();
//     int i, p=0;
    
//     while(p<=dt-dw) {
//         i=0;
//     while(i<dw && w[i]==t[p+i]) i++;
//         if(i==dw)
//             return p;
//         else 
//             p++;
//     }
//     cout<<"Wzorzec nie występuje w tekście"<<endl;
//     return -1;
// }

int main() {
    string t, w;
    cout<<"Podaj tekst: ";
    cin>>t;
    cout<<"Podaj wzorzec: ";
    cin>>w;
    int dw=w.size();
    int dt=t.size();
    int i, p=0;
    
    while(p<=dt-dw) {
        i=0;
    while(i<dw && w[i]==t[p+i]) i++;
        if(i==dw)
            return p;
        else 
            p++;
    }
    if(p==dt)
        cout<<p;
    else
        cout<<"Wzorzec nie występuje w tekście"<<endl;

    return -1;
}